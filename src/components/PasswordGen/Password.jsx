import React from 'react'
import { CreatedPassword } from '../styledComponents/CreatedPassword'

const Password = ({text}) => {
  return (
    <CreatedPassword>{text}</CreatedPassword>
  )
}

export default Password